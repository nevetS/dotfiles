# emacs

| Platform | Status          | Version |
|----------|-----------------|---------|
| Linux    | ✅ Full          | —       |
| macOS    | ✅ Full          | —       |
| Windows  | ⚠️ Partial       | —       |

> Update the Version column with the output of `emacs --version` on each platform.
> Status: ✅ Full | ⚠️ Partial | ❌ Not supported | 🚧 In progress

## Features

- [ ] TODO: document installed packages/layers
- [ ] TODO: note any OS-specific config blocks

## Local overrides

None currently. Add `~/.emacs.d/local.el` and load it from `init.el` if needed.

## Install path

| Platform | Path |
|----------|------|
| Linux    | `~/.emacs.d/` |
| macOS    | `~/.emacs.d/` |
| Windows  | `%APPDATA%/.emacs.d/` |

## TODO

- [ ] Document all packages in use
- [ ] Test Windows compatibility
